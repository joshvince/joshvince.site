<% content_for :content do %>
<div class="bg-gray-50 h-screen">
  <div
    class="container mx-auto max-w-5xl md:pt-12 pb-12 flex flex-col md:flex-row font-body bg-gray-50 text-gray-600 font-light text-lg w-100"
    id="info-container"
  >
    <div class="px-6 md:px-12 mt-24 md:mt-0 md:w-9/12 md:ml-64">
      <%= content_for?(:body_content) ? yield(:body_content) : yield %>
    </div>
  </div>
</div>
<% end %>

<%= render template: "layouts/application" %>
